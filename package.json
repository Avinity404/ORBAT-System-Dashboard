{
  "name": "orbat-system-root",
  "version": "1.0.0",
  "scripts": {
    "server": "npx json-server --watch db.json --port 5000",
    "frontend": "npm run dev --prefix frontend",
    "start": "npx concurrently \"npm run server\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}